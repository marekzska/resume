// Shared motion constants live in the warm-motion package (dogfooded here).
export { EASE, REVEAL_DURATION, REVEAL_RISE, REVEAL_STAGGER } from 'warm-motion'

// Site-specific timing for the hero + header intro.
export const HERO_DURATION = 0.8
export const HERO_RISE = 24
export const HERO_DELAY = 0.1
export const NAV_DELAY = 1.0
export const NAV_DURATION = 0.4
